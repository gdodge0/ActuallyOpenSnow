# FastAPI Backend Dependencies
fastapi
uvicorn[standard]
pydantic
python-dotenv

# Rate limiting
slowapi==0.1.9

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
redis>=5.0.0
alembic>=1.13.0

# Reference the weather package (installed in editable mode)
# Run: pip install -e ../weather

# CORS for frontend development
# (included in fastapi)

